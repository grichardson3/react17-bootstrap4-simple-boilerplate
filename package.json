{
  "name": "indecision-app",
  "version": "1.0.0",
  "description": "",
  "main": "./src/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src/js/app.js --out-file=public/scripts/app.js --presets=env,react --watch"
  },
  "author": "Gareth Richardson",
  "license": "ISC",
  "dependencies": {
    "babel-preset-env": "^1.7.0",
    "babel-preset-react": "^6.24.1",
    "bootstrap": "^4.5.3"
  }
}
